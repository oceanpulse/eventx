import { GlobalState } from "@/utils/type.dt"

export const globalStates: GlobalState = {
    event: null,
    tickets: [],
    ticketModal: 'scale-0',
}

//these are the states we will manage globally